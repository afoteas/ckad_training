apiVersion: v1
kind: ConfigMap
metadata:
  name: app-live-config
  labels:
    app: reloader-demo
data:
  # CHANGED VALUE: This will trigger the restart when applied
  app.version: "2.0.0" 
  
  # Log level remains the same
  log.level: "INFO" 
