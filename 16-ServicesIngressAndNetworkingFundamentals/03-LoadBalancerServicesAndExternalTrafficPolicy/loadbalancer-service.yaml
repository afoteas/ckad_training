apiVersion: v1
kind: Service
metadata:
  name: web-lb
spec:
  type: LoadBalancer
  externalTrafficPolicy: Cluster   # or Local to preserve client source IP
  selector:
    app: web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
