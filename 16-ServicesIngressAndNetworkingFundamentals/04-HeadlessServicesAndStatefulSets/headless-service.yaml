apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  clusterIP: None        # headless: no load-balanced IP
  selector:
    app: mysql
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
