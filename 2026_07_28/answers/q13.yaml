apiVersion: v1
kind: Service
metadata:
  labels:
    app: api
  name: api-headless
  namespace: app-net
spec:
  clusterIP: None
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: api
  type: ClusterIP
