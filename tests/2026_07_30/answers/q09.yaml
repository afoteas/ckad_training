apiVersion: v1
data:
  pass: czNjcjN0
  user: YWRtaW4=
kind: Secret
metadata:
  name: db-cred
  namespace: config
---
apiVersion: v1
kind: Pod
metadata:
  labels:
    run: sec-consumer
  name: sec-consumer
  namespace: config
spec:
  containers:
  - image: nginx:1.25
    name: sec-consumer
    volumeMounts:
      - name: secret-volume
        readOnly: true
        mountPath: /etc/db
  restartPolicy: Never
  volumes:
    - name: secret-volume
      secret:
        secretName: db-cred
