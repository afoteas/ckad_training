apiVersion: v1
kind: Pod
metadata:
  name: self-aware
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command: [ "sleep", "3600" ]
      env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
  restartPolicy: Never
