apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-tier
  name: web-tier-svc
  namespace: edge
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: web-tier
status:
  loadBalancer: {}
